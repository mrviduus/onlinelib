# Mobile App — Bug Register

Полный срез багов и UX-дефектов мобильного приложения TextStack (Expo SDK 55 + React Native 0.83.2). Документ делится на три раздела: уже исправленное в этой и предыдущей сессиях, активные дефекты (с приоритетом P0–P3) и риски, требующие более глубокой проработки.

---

## 1. Уже исправлено

| ID | Область | Файл | Суть и решение |
|----|---------|------|----------------|
| B-01 | Reader / Selection | `app/reader/[bookSlug]/[chapterSlug].tsx` | WordCard позиционировался под футером при скрытом чтение-прогрессе — сделан `bottomOffset={footerHeight}`, не перекрывается. |
| B-02 | Reader / Highlights | `src/components/HighlightNoteModal.tsx` | На Android `Alert.prompt` был no-op; заменён на кросс-платформенный модал. |
| B-03 | Auth | `app.config.ts` / `AuthContext.tsx` | Google Sign-In padал из-за отсутствия webClientId в EAS config — добавлен. |
| B-04 | Reader / WebView | `src/lib/readerHtml.ts` | iOS `selectionchange` гонка — добавлен дебаунс + generation id. |
| B-05 | Dictionary/Translate | `useTargetLanguage.ts` | Язык цели брался из UI-locale, не из native language — добавлен хук. |
| B-06/07 | WordCard | `WordCard.tsx` | Некорректный выбор `from`-языка для определения/перевода. |
| B-08 | Auth | `src/context/AuthContext.tsx` | SecureStore fallback на web-сборках вылетал — вынесен в storage abstraction. |
| B-09 | Reader | `ReaderStatsWidget.tsx` | 1-секундный setInterval не чистился при размонтировании. |
| B-10 | Library/Vocab | `useAsyncResult.ts` | Race между вкладками библиотеки/словаря — универсальный хук с generation counter. |
| B-11 | Reader Settings | `ReaderSettingsDrawer.tsx` | Switch на Android рендерился невидимым — добавлены `trackColor`/`thumbColor`/`ios_backgroundColor`. |
| B-12 | Reader / Selection | `[chapterSlug].tsx`, `WordCard.tsx` | Повторный тап по тому же слову не сбрасывал auto-dismiss таймер — добавлен `selectionId`. |
| B-13 | Reader / WebView | `readerHtml.ts` | Длинные немецкие/украинские слова (>40) обрезались — лимит поднят до 80. |
| B-14 | TOC Sheet | `TocSheet.tsx` | `getItemLayout` врал при wrap'нутых заголовках — заменён на onScrollToIndexFailed. |
| B-15 | Home | `app/(tabs)/index.tsx` | Greeting slot замерзал из-за `useMemo([])` — заменено на `useState` + 60s interval. |
| B-16 | Navigation | `app/(tabs)/_layout.tsx` | Tab bar не учитывал safe-area bottom на iOS с home-indicator. |
| B-17 | Toast | `src/context/ToastContext.tsx` | Toast перекрывал таб-бар — добавлен `bottomOffset` + `defaultBottomOffset`. |
| B-18 | Profile / Avatar | `profile.tsx`, `packages/shared/src/api/auth.ts` | Единая копия "Upload failed" — теперь 413/415/401/5xx маппятся на конкретные сообщения. |
| B-19 | Search | `(tabs)/search.tsx` | Устаревшие ответы поиска затирали свежие — добавлен generation ref. |
| B-20 | ContinueReading | `components/ContinueReadingCard.tsx` | Сегменты роута `/my-books/read/[id]/[slug]` были перепутаны местами — пользователь не мог продолжить чтение. Исправлено. |
| B-21 | Reader / Bars | `readerHtml.ts` | Чтобы показать бары, требовалось ~20px хода вверх после перелома (baseline reset + threshold). Теперь любой первый пиксель вверх после down-рана показывает бары мгновенно, UP-threshold 6, DOWN-threshold 48. |
| B-22 | My Books polling | `app/my-books/[id].tsx` | `setInterval` молотил API каждые 5с даже при сбоях и разыменовывал `id!`. Переписан на recursive `setTimeout` с экспоненциальным backoff (5s → 10 → 20 → 40, cap 60), stop-on-terminal-status, unmount guard, toast после 3 подряд неудач. |
| B-23 | Search stale closure | `app/(tabs)/search.tsx` | `doSearch` замыкал устаревший `language` при смене native-lang. Перевели `saveRecent`/`removeRecent`/`clearRecent` на functional setState, `doSearch` теперь зависит только от `[language, saveRecent]`, `useEffect` тоже обновлён. |
| B-24 | Upload XHR | `app/my-books/upload.tsx` | При unmount запрос продолжался; network/413/415/401/5xx давали одно сообщение. Добавлены `xhrRef`-abort, Cancel-кнопка, `xhr.onabort` → `err.aborted=true`, `uploadErrorMessage(status)` маппер. |
| B-25 | Book detail error | `app/book/[slug].tsx` | Сбой `getBook` оставлял пустой экран. Добавлены `fetchError` state, UI с Retry/Go back, optimistic library toggle с rollback, wire `retryFailed` для failed chapters. |
| B-26 | Download retries | `src/context/DownloadContext.tsx` | Одна упавшая глава останавливала цикл. Добавлены `downloadChapter` с 3 попытками (400→1200→2400ms), трекинг `failedChapterSlugs`, `retryFailed(editionId)` только для недокаченных глав. |
| B-27 | Selection a11y | `src/components/SelectionActionBar.tsx` | Все `TouchableOpacity` без `accessibilityLabel`/`accessibilityRole`. Добавлены осмысленные labels ("Copy selection", "Look up in dictionary", "Translate selection", "Read selection aloud", "Mark word as known", "Highlight in {color}"), `accessibilityState` для speak/save. |
| B-28 | TOC scroll failure | `src/components/TocSheet.tsx` | `onScrollToIndexFailed` был no-op. Пробросили `listRef`, делаем `scrollToOffset(approxOffset)` → через 100ms retry `scrollToIndex(index)`. |
| B-29 | Bookmark rollback | `app/reader/[bookSlug]/[chapterSlug].tsx` | `deleteBookmark`/`toggleBookmark` снимали закладку из state даже при fail API. Теперь snapshot → optimistic remove → rollback + toast on error. |
| B-30 | Toast dead API | `src/context/ToastContext.tsx` | `setDefaultBottomOffset` экспортировался, но никуда не подключался. Удалён из контекста; per-call `bottomOffset` остался как основной способ. |
| B-31 | Haptics premature | `src/hooks/useHaptics.ts` | `enabled.current = true` до резолва AsyncStorage — буз даже при отключённой хаптике. Теперь дефолт `false` до загрузки; `toggle()` тоже помечает loaded. |
| B-32 | Reader effect deps | `app/reader/[bookSlug]/[chapterSlug].tsx` | Highlights/vocab effects зависели от `chapter` (reference), перезагружались при каждом refetch той же главы. Переключили на `chapter?.id` + cancellation flag. |
| B-33 | Shared API offline | `packages/shared/src/api/client.ts` | `fetch`-ошибки (offline/DNS) прилетали как сырой `TypeError`. Добавили `safeFetch` → `ApiError(0, …)` с `isNetworkError=true`; `errorFromResponse` читает body, чтобы после refresh-retry возвращать реальный статус/сообщение. |
| B-34 | Mobile refresh flow | `src/lib/api.ts` + `src/lib/authEvents.ts` + `AuthContext.tsx` | Server-reject refresh (4xx) молча чистил токены, но UI оставался "залогинен" и спамил 401. Ввели шину `authEvents`, latch против флуда, AuthContext чистит `user` только если мы всё ещё считались залогинены. Network-fail на refresh больше не выносит токены. |
| B-35 | Reading session lifecycle | `src/hooks/useReadingSession.ts` | Смена книги в пределах одного mount'а не ресетила `submittedRef`/счётчики — новая сессия никогда не сабмитилась. Heartbeat молотил и после сабмита. Теперь `sessionKey` effect ресетит стейт на смене книги, heartbeat no-op после сабмита, `updateProgress` не рaзоружает закрытую сессию. |
| B-36 | Reader HTML memoization | `app/reader/[bookSlug]/[chapterSlug].tsx` | `buildReaderHtml` пересчитывался на каждом рендере (bars toggle, progress tick) — CPU + потенциальный WebView reload. Обернул в `useMemo`, `source` тоже стабилизирован. |
| B-37 | Logout cache leak | `src/context/DownloadContext.tsx` | После sign-out в памяти оставались `downloads` Map и `cachedBooks` от прошлого пользователя. Теперь при `isAuthenticated: true → false` отменяем in-flight downloads и сбрасываем state (disk-cache остаётся — ключ по editionId, не user). |
| B-38 | Offline chapter listing | `src/lib/offlineDb.ts` | Не было способа перечислить закешированные главы для edition-а. Добавлен `listCachedChapters(editionId)` → `CachedChapterSummary[]` (slug/title/wordCount, сортировка по cachedAt для приблизительного сохранения порядка). |
| B-39 | Reader chapter race + offline UX | `app/reader/[bookSlug]/[chapterSlug].tsx` | (1) chapter-fetch effect не имел cancellation — быстрая смена глав могла записать устаревший chapter. (2) cache-hit путь не обновлял `wordCountRef.current` → ETA считался от предыдущей главы. (3) cache-miss оставлял `loading=false` + `chapter=null` → бесконечный спиннер. Теперь: `let cancelled = false`, `wordCountRef` апдейтится в обе ветки, новое state `chapterError: 'offline' \| 'notfound'` рендерит cloud-offline / help-circle UI с кнопкой "Go back". Book-resolution effect тоже получил cancellation + offline-fallback ставит `bookTitleRef`/setBookTitle. |
| B-40 | Book detail offline fallback | `app/book/[slug].tsx` | Любой network-fail сводил страницу к «Couldn't load this book», даже если книга полностью скачана. Теперь на catch (кроме 404) читаем `getAllCachedBooks()` + `listCachedChapters()` + `getLocalProgress()`, собираем минимальный `BookDetail` из cache, ставим `offlineMode=true`, рендерим баннер «You're offline» и прячем Rating/Moods/Reviews/EPUB/Library-toggle (им нужен сервер). Continue-slug поднимается из локального прогресса. |
| B-41 | Login keyboard polish | `app/(auth)/login.tsx` | Форма логина не имела `returnKeyType`/`textContentType`/autofill — iOS Keychain/Autofill не подцеплялись, Enter на последнем поле не отправлял форму, двойной тап на «Sign in» мог выстрелить два раза. Добавлены `emailRef`/`passwordRef`, `returnKeyType` ("next"/"go") + `onSubmitEditing` chain, `textContentType` (name/emailAddress/newPassword/password), `autoComplete` (password-new/password по режиму), `autoCorrect={false}`, `if (loading) return` guard в handleEmailAuth. |
| B-42 | Vocab reminder race | `src/lib/vocabReminder.ts` | VocabularyReviewCard focus-effect мог вызвать `schedule()` после того как профиль только что сделал `cancel()` — и наоборот. Notification-id терялся, уведомление либо дублировалось, либо не отменялось. Теперь `schedule()`/`cancel()` сериализуются через общий promise-chain queue (`opChain`), порядок операций гарантируется независимо от экрана. |
| B-43 | Logout cache leak | `src/context/AuthContext.tsx` + `src/lib/progressStorage.ts` | После sign-out в AsyncStorage оставались `vocab.stats.last.{userId}` и `reading.progress.{editionId}` — при входе другим юзером UI секунду показывал чужие цифры. Добавлены `clearVocabStatsCache()` и новый `clearAllLocalProgress()`; вызов обоих в `signOut()` и в `onAuthFailure` (catch no-op). |
| B-44 | TTS language hardcoded | `src/hooks/useTts.ts` + все call-sites (`reader/...`, `my-books/read/...`, `vocabulary.tsx`, `vocabulary/review.tsx`) | `useTts` всегда озвучивал через `en-US`, даже украинский текст. Добавлен `toBcp47(lang)` маппер (`en`→`en-US`, `uk`→`uk-UA`), сигнатура `speak(text, opts: TtsSpeakOptions \| number)` с back-compat для rate-only вызовов, `trackTtsPlayed({ language })` теперь передаёт реальный язык. Вызовы обновлены: reader — `{ rate, lang: language }`, vocabulary list — `{ lang: item.language }`, review — `{ lang: language }` (ReviewCardDto language pending backend). |
| B-45 | TTS unmount mid-speech | `src/hooks/useTts.ts` | Навигация с читалки во время `Speech.speak()` оставляла голос звучать поверх следующего экрана. Добавлен `speakingRef` + cleanup effect `return () => { if (speakingRef.current) Speech.stop() }`. |
| B-46 | Vocabulary review setState after unmount | `src/hooks/useVocabularyReview.ts` | `getReviewQueue` / `submitReview` могли резолвнуться уже после того как пользователь ушёл с экрана — React ругался warning'ом + мы тихо воскрешали сессию. Добавлен `mountedRef = useRef(true)` + effect, `if (!mountedRef.current) return` перед каждым setState в success/catch ветках `startSession` и `submitAnswer`. |
| B-47 | QuickStats cross-user leak | `src/hooks/useQuickStats.ts` | При sign-out хук не чистил `stats`, а при следующем sign-in новая promise могла проиграть старой. Теперь `!isAuthenticated` → `setStats(null)` + early return, promise обёрнута в `let cancelled = false` + cleanup. |
| B-48 | Reader settings async load race | `src/hooks/useReaderSettings.ts` | `AsyncStorage.getItem` + legacy v1 migration могли отрезолвиться после unmount или после того как пользователь успел нажать «A+» на дефолте — его правка затиралась. Добавлен `cancelled` flag на оба await'а (raw read + legacy fallback). |
| B-49 | P3 accessibility sweep | `src/components/{BookmarksSheet,DictionarySheet,TocSheet,TranslationSheet,HighlightNoteModal,ReaderSettingsDrawer}.tsx` | Сit-sheets и модалки не имели `onRequestClose` (Android back кнопка не закрывала), большинство close/speak/delete/row-кнопок — без `accessibilityRole`/`Label`/`State`. Пройдено по всем шести компонентам: Modal → `onRequestClose`, заголовки → `role="header"`, все TouchableOpacity/Pressable → `role="button"` + осмысленный label + `state.selected` для chip/row тогглов (TOC row, font/theme/align/TTS speed chips, toggle-bookmark). TextInput highlight note получил `accessibilityLabel`, ActivityIndicator — "Loading definition"/"Translating", error Text — `role="alert"`. |
| B-50 | Blog detail race + like rollback | `app/blog/[slug].tsx` | Load-effect не имел cancellation (user мог back-жестом уйти до резолва → setState after unmount warning). Like-тоггл без rollback: сеть роняется → UI показывает like, бекенд — нет. Comment/reply не имели submit-guard против двойного тапа. Добавлен `let cancelled = false` + cleanup, optimistic like с snapshot `{prevLiked, prevCount}` и rollback в catch, `if (submitting) return` в handleComment/handleReply, все catches логируются через `console.warn`. |
| B-51 | Blog list onEndReached race | `app/blog/index.tsx` | `FlatList.onEndReached` на коротком списке/быстром скролле стрелял `fetchPosts` 3-5 раз подряд → дубликаты в state. Смена `language`/`tag` параллельно с in-flight запросом затирала свежий ответ старым. Добавлены `loadingMoreRef` (guard от re-entrance при offset>0) и `genRef` (generation counter) — stale response просто `return`, не `setState`. `fetchPosts` обёрнут в `useCallback([language])`, `useEffect` deps исправлены. |
| B-52 | Highlights review unmount | `app/highlights/review.tsx` | `getHighlightsForReview` мог зарезолвиться уже после back-жеста → warning о setState on unmounted. Silent catch на `markHighlightReviewed` терял burst of failures. Добавлен `cancelled` flag на load-effect и `console.warn` на оба catch-а (load, mark-reviewed). |
| B-53 | Profile name input polish | `app/(tabs)/profile.tsx` | TextInput редактирования имени не имел autofill/автокорректных аттрибутов — iOS автокоррект ломал валидные имена, Enter не сохранял, a11y-label отсутствовал. Добавлены `autoCorrect={false}`, `textContentType="name"`, `autoComplete="name"`, `returnKeyType="done"`, `onSubmitEditing={saveProfile}`, `accessibilityLabel="Edit display name"`. |
| B-54 | Vocabulary delete + fetch guards | `app/(tabs)/vocabulary.tsx` | `handleDelete` вычищал слово из UI до подтверждения API — сетевая ошибка оставляла UI в рассинхроне с сервером. `onEndReached` мог стрелять конкурентно. `handleEdit` тихо глотал ошибки. Теперь optimistic delete с `snapshot`/`snapshotTotal` rollback + toast "Could not delete word", `loadingMoreRef` guard в loadData, `useToast` на edit/delete errors. |
| B-55 | Library silent-catch sweep | `app/(tabs)/library.tsx` | Polling `getUserBooks` и `libraryApi.removeFromLibrary`/`readingProgressApi.updateProgress` из SavedList.handleAction, плюс UploadsList (Mark as read/unread, Retry, Cancel upload, Delete book), плюс storage-quota fetch — все глотали ошибки тихо. Добавлены `useToast` в LibraryScreen и SavedList, `console.warn` на polling/quota, optimistic remove + rollback + toast для "Remove from Library", универсальный `runAction(fn, label)` helper в UploadsList логирует + показывает toast "${label} failed. Try again." |
| B-56 | User-book reader HTML memoization | `app/my-books/read/[bookId]/[chapterSlug].tsx` | Та же проблема, что B-36 на публичном reader: `buildReaderHtml` пересчитывался на каждом рендере (bars toggle, progress tick, selection change), и `source={{ html }}` давал каждый раз новый объект — WebView на Android мог "прыгать" в начало главы. Обернули build в `useMemo` по контентным deps (`chapter`, шрифты, тема, insets), `source` тоже вынесли в `useMemo({ html })`. |
| B-57 | User-book reader silent catches | `app/my-books/read/[bookId]/[chapterSlug].tsx` | ~10 silent `catch` блоков (bookmarks load, chapter list, progress save, auto-save word, handleSaveWord/MarkKnown/Highlight, highlights load, vocab map, loadNextChapter, highlight note save/delete, toggleBookmark, deleteBookmark) — ошибки исчезали. Пройдено по всем: `console.warn` везде, `showToast({ variant: 'error' })` на user-actionable (save word/mark known/highlight/note save/note delete), `console.error` → `console.warn` для согласованности с остальным приложением. |
| B-58 | Stats screen cancellation + goal errors | `app/stats/index.tsx` | `loadData` делал `Promise.all` из 7 endpoint-ов без cancellation — back-жест на полуфетче давал warning «setState on unmounted» и мог перезаписать свежий язык/год устаревшим ответом. Goals `handleCreate`/`handleDelete` молча глотали ошибки, оставляя `setSaving(true)` залипшим. Добавлен `genRef` generation counter + per-branch check перед каждым setState, `handleCreate` теперь `try/finally` c toast + log, `handleDelete` тоже с toast + log. |
| B-59 | Home cancellation + silent catches | `app/(tabs)/index.tsx` | `loadContent`/`loadMoods` не имели cancellation — быстрое переключение EN↔UK давало гонку: ответ на старый язык прилетал после нового и перетирал stateю. Оба catch-а были silent (`// Empty states already handle`). Добавлены `booksGenRef` и `moodsGenRef`, `setState` гардятся по generation, catches логируются `console.warn`. |
| B-60 | Highlights index race + onEndReached | `app/highlights/index.tsx` | Переключение фильтров (sort / color / bookType / search) не имело generation counter — старые ответы перетирали новые. `onEndReached` мог стрелять 3-5 раз подряд на коротком списке, создавая дубликаты. `console.error` не соответствовал конвенции. Добавлены `genRef` + `loadingMoreRef`, generation-check перед setState, onEndReached-guard, `console.error` → `console.warn`. |
| B-61 | Books/Authors search debounce | `app/books.tsx`, `app/authors.tsx` | Поиск фильтровал на каждом keystroke — 8 символов = 8 запросов + 8 стэйл-ответов, которые generation counter просто выбрасывал (в books.tsx был, в authors.tsx — нет). Добавлен 300ms debounce на query (как в search/highlights), `lastFetchRef` в authors.tsx, `console.error` → `console.warn`, генерация гардит `setLoading(false)` от устаревших веток. Genre list fetch в books.tsx тоже получил generation-counter, чтобы смена language не оставляла старые жанры. |
| B-62 | Author/Genre detail error UX | `app/author/[slug].tsx`, `app/genre/[slug].tsx` | Оба экрана уходили в вечный skeleton при network/404 — `loading || !author` держал skeleton навсегда, без retry/back. Плюс fetch не имел cancellation: быстрая смена slug или language → старый ответ перезаписывал новый. Добавлены `let cancelled = false` cleanup, `error: 'notfound' \| 'network'` state, `retryNonce` для ручного retry, UI с иконкой (help-circle / cloud-offline) + Retry/Go-back кнопками. Share URL в author теперь уважает текущий `language` вместо захардкоженного `/en/`, share-fail даёт `console.warn`. `console.error` → `console.warn` по всему. |
| B-63 | Genres list cancellation | `app/genres.tsx` | Фетч не имел cancellation и не ресетил `setLoading(true)` при смене `language` — переключение EN↔UK оставляло старые жанры, а затем их перезатирал устаревший ответ. Добавлены `let cancelled = false`, `setLoading(true)` на каждом ран effect-а, `console.error` → `console.warn`. |
| B-64 | Reset password polish | `app/reset-password.tsx` | `onSubmitEditing` мог выстрелить при loading-е (не было guard-а), setState после unmount если пользователь ушёл с экрана во время resolve. TextInput не имел `returnKeyType`/`textContentType="newPassword"`/`autoComplete="password-new"`/`accessibilityLabel` — iOS Keychain не предлагал пароль. Добавлены `mountedRef` cleanup, `if (loading) return` guard, полный набор autofill attributes + `returnKeyType="go"`, console.warn на fail. |
| B-65 | Vocab review haptic in render | `app/vocabulary/review.tsx` | `haptics.play('complete')` вызывался внутри render-а при `isSessionComplete` — React пере-рендерил компонент на каждом тике/нажатии, и хаптик-фидбек триггерился много раз подряд. Вынесен в `useEffect` c derived `sessionComplete` state, fires ровно один раз на переход. |
| B-66 | Reader silent catches sweep | `app/reader/[bookSlug]/[chapterSlug].tsx` | Public reader имел остатки silent catches: `handleSaveWord` (line 483), `handleMarkKnown` (line 496), `handleHighlight` (`console.error`), HighlightNoteModal `onSave`/`onDelete`. Все user-actionable catches теперь `console.warn` + `showToast({ variant: 'error' })` с осмысленным сообщением ("Could not save word", "Could not mark as known", "Could not add highlight", "Could not save note", "Could not delete highlight"). Fire-and-forget translation enrich теперь тоже логируется. `Offline cache read` `console.error` → `console.warn` для согласованности. |
| B-67 | ContinueReadingCard refocus + a11y | `src/components/ContinueReadingCard.tsx` | `useEffect(..., [])` грузил "последнее читаемое" только один раз — если пользователь на другом табе сохранил/продолжил другую книгу, карточка на home не обновлялась до ре-маунта экрана. Плюс silent-catch (карточка просто исчезала), и PressableScale без a11y label. Переведён на `useFocusEffect(useCallback(...))` с `cancelled`-flag на каждом `setState`, `console.warn` на catch, `accessibilityRole="button"` + `accessibilityLabel` с названием книги и процентом. |
| B-68 | Reviews fetch + like/comment polish | `src/components/reviews/ReviewsSection.tsx`, `reviews/ReviewForm.tsx`, `reviews/ReviewCard.tsx`, `reviews/StarRatingInput.tsx`, `reviews/RatingDistribution.tsx` | `ReviewsSection.fetchAll` не имел generation counter → смена `sort`/`ratingFilter` могла оставить старый ответ поверх нового. `loadMore` по `onEndReached` на короткой сетке стрелял 3-5 раз подряд → дубли. Like/unlike молча проглатывали сеть. `ReviewForm` не гардил setState от unmount. `ReviewCard.handleLike` при network-fail оставлял `helpfulCount = -1` (без Math.max clamp), `toggleComments`/`submitComment` — silent. Добавлены `fetchGenRef` + `loadingMoreRef` + `useCallback`, `mountedRef` в ReviewForm, `console.warn` + `useToast` в ReviewCard для всех catches, `Math.max(0, c-1)` при rollback, `accessibilityState` на like/comments toggles, `accessibilityState.selected/disabled` на StarRatingInput, `accessibilityLabel` на RatingDistribution. Удалён неиспользуемый `Ionicons` импорт. |
| B-69 | ReaderTapCoachmark animated loop leak | `src/components/reader/ReaderTapCoachmark.tsx` | `Animated.loop(...).start()` запускался в effect'е, но его handle нигде не хранился → на unmount или на пользовательский dismiss loop продолжал тикать в фоне (микро-утечка + зря будит JS-поток). Добавлен `pulseRef = useRef<Animated.CompositeAnimation \| null>(null)`, handle сохраняется при старте, в cleanup и в dismiss вызывается `pulseRef.current.stop()` + сброс ref. |
| B-70 | Root layout dev noise + splash hide | `app/_layout.tsx` | `console.log('[TextStack] RootLayout mounted...')` и `console.log('[TextStack] Hiding splash...')` работали и в prod-билде — шум в Flipper/Reactotron логах реальных пользователей. Плюс `SplashScreen.hideAsync()` без `.catch` мог ронять unhandled promise rejection на платформах, где splash не зарегистрирован или уже скрыт. Обёрнут `console.log` в `if (__DEV__)`, добавлен `.catch(e => console.warn('Splash hide failed:', e))`. |
| B-71 | Accessibility sweep: home + vocab + focus reader + profile | `src/components/home/{CollectionsCarousel,HorizontalBookList,QuickActionGrid}.tsx`, `src/components/vocabulary/{FlashCard,MultipleChoiceCard,NewWordCard,ReviewFeedback,SessionSummary}.tsx`, `src/components/focus/FocusReader.tsx`, `src/components/profile/VocabReminderSettingsRow.tsx`, `src/components/ui/PressableScale.tsx`, `app/+not-found.tsx` | PressableScale не пробрасывал `accessibilityState` — невозможно было сообщить screen-reader'у про `selected`/`disabled` на кнопках выбора в SessionSummary/MultipleChoiceCard (TS2322 после попытки передать проп). Расширил интерфейс `PressableScaleProps.accessibilityState?: AccessibilityState` и передал в `<Pressable>`. Mood-карточки, "View All", quick-action tiles, flashcard flip/assess buttons, MC options, NewWord Continue, SessionSummary mode toggle + batch chips, NotFound Go Home — получили `accessibilityRole="button"` + осмысленный `accessibilityLabel` + `accessibilityState` где релевантно. ReviewFeedback dictionary lookup получил cancellation flag + `console.warn` на fail + сброс `setFetchedDef(null)` при смене слова. FocusReader — `console.warn` на AsyncStorage read/persist/translate catch'и. VocabReminderSettingsRow — cancellation на initial settings load + `console.warn` на fail. |
| B-72 | WordCard translation race + a11y | `src/components/WordCard.tsx` | `useEffect` для перевода не имел cancellation — быстрый re-tap по разным словам мог оставить ответ для слова N поверх карточки слова N+1 (race). Плюс silent `.catch(() => setTranslation(''))` скрывал сеть, `(res: any)` — unsafe-cast. Главный `<TouchableOpacity>` (expand/collapse) и все вложенные кнопки (TTS, Save, MarkKnown, highlight-цвета, Dictionary, Dismiss) не имели `accessibilityRole`/`Label`/`State` — screen-reader не мог управлять карточкой вообще. Добавлен `let cancelled = false` на fetch, `console.warn` на catch, типизирован ответ `{ translatedText?, translation? }`. Полный a11y-проход: expand-кнопка с `state.expanded`, TTS с `state.selected`, Save/MarkKnown с осмысленным label, savedBtn → `role="image"`, highlight-цвета → "Highlight in {key}", Dictionary/Dismiss — button labels. ActivityIndicator "Translating" label. |
| B-73 | StarRating no rollback + a11y | `src/components/StarRating.tsx` | Первоначальный fetch и save без cancellation/mountedRef — быстрое переключение книг или unmount во время save давал warning + race. Сеть ронялась → UI показывал новый рейтинг, сервер ничего не знал (silent `} catch {}`). Нет rollback, нет toast, нет a11y — screen-reader слышал пять пустых кнопок. Полный переписал: `mountedRef`, optimistic update с `previousRating` snapshot, rollback + `toast.show({ variant: 'error' })` на fail, cancellation flag на initial fetch (`r ? r.rating : 0` корректно сбрасывает на смене книги), `console.warn` на load/save catches, `accessibilityRole="button"` + `accessibilityLabel="Rate N stars"` + `accessibilityState={{ selected: filled, disabled: saving }}` на каждой звезде, контейнер получил `accessibilityLabel="Rated N of 5 stars"`. |
| B-74 | MoodSelector silent catches + a11y | `src/components/MoodSelector.tsx` | Оба load-effect'а и toggle-ветка `} catch {}` были silent — сеть упала, UI в рассинхроне с сервером. Ни одного `console.warn`. Mood-чипы без `accessibilityRole`/`Label`/`State` — screen-reader не мог отличить selected/disabled. Per-book fetch не имел cancellation (быстрый book-switch → stale ids). Optimistic toggle полагался на snapshot через closure, что работало только благодаря saving-guard'у — сделал snapshot явным через `new Set(selected)`. `mountedRef` guard на save, `console.warn` на все catches, `toast.show({ message: 'Could not save mood', variant: 'error' })` на rollback. Чипы: `accessibilityRole="button"` + `accessibilityLabel="Mood: {name}"` + `accessibilityState={{ selected, disabled }}`. |
| B-75 | LanguagePickerModal + ReaderSearchBar a11y | `src/components/LanguagePickerModal.tsx`, `src/components/ReaderSearchBar.tsx` | LanguagePickerModal: заголовок "Select language" и секции "Popular"/"All languages" не имели `accessibilityRole="header"`, close-кнопка — без label, search-input без `accessibilityLabel` + `returnKeyType`, языковые строки — без `role="button"`/`state.selected`/осмысленного label (screen-reader не мог сказать, что язык уже выбран). ReaderSearchBar: TextInput без label/`autoCorrect/autoCapitalize` + без `fontFamily` (рассинхрон с остальными input'ами), счётчик матчей `1/5` не озвучивался нормально, prev/next/close-кнопки — без role/label/disabled-state. Добавлены все недостающие a11y-атрибуты + `accessibilityLabel="Match N of M"` на счётчике, `fontFamily: fonts.sans` на input. |
| B-78 | ReviewFeedback mini "Next" tap-target too small | `src/components/vocabulary/ReviewFeedback.tsx` | В classic-режиме practice-экрана mini-feedback карточки весь layout — `alignItems: 'center'`, из-за чего `nextBtn` схлопывался до content-width: крошечная кнопка "Next" на фоне большой пустой карточки. Пользователь жаловался на это (screenshot "Practice 1/10" с маленькой кнопкой). Добавил отдельный стиль `miniNextBtn` с `alignSelf: 'stretch'` — кнопка теперь тянется на всю ширину карточки, tap-target = вся ширина, визуальный вес совпадает с blitz-mode CTA. Padding оставлен `paddingVertical: 14` (в норме для material-style action button). |
| B-77 | **REGRESSION**: Reader hooks-after-early-return | `app/reader/[bookSlug]/[chapterSlug].tsx` | **Критическая регрессия из B-36 (R-3 memoization)**: `useMemo(html, …)` и `useMemo(webViewSource, …)` стояли ПОСЛЕ ранних `if (loading) return` и `if (!chapter) return`. На loading-кадре React видел N хуков, на loaded-кадре N+2 → "Rendered more hooks than during the previous render" → ErrorBoundary размантывал весь `<ReaderScreen>`. Каскад симптомов, на которые жаловался пользователь: (1) подчёркивание/выделение слова открывало нативное Android-меню "Define / Copy / Share" вместо `WordCard` — потому что WebView и оверлей-слои реактного UI вылетали вместе с экраном; (2) поп-ап `WordCard` не появлялся; (3) перевод не работал — потому что `handleMessage` callback'а уже не существовало; (4) виден экран "Something went wrong / Try Again". Фикс: оба `useMemo` подняты ВЫШЕ early-returns, `html` теперь фоллбэк `chapter ? buildReaderHtml(...) : ''`, deps переключены с `chapter.html` на `chapter?.html`. Добавлен длинный inline-комментарий объясняющий, почему хуки ОБЯЗАНЫ быть до return'ов и почему именно эта ошибка вызывала такой набор симптомов — чтобы будущая аудиторская правка не вернула регрессию. `tsc --noEmit` → exit 0. |
| B-76 | EPUB/Share Linking + web-deprecation align | `app/book/[slug].tsx`, `app/my-books/[id].tsx` | R-6 при ближайшем рассмотрении оказался не тем, чем казался: мобилка не использует `expo-sharing`/`expo-file-system` вообще — EPUB-экспорт работает через `Linking.openURL` в системный браузер. Зато вскрылись три реальные проблемы. (1) **Public-book "Download EPUB" на `book/[slug].tsx`** — на web этот путь deprecated 2026-04-15 (см. CLAUDE.md: "UI anchor is gone, backend route still live but unreachable"), а на мобилке кнопка оставалась. Удалил кнопку (align UX с web, чтобы не тыкать пользователя в dead-path), оставил JSDoc-комментарий с инструкцией, как вернуть. (2) **Silent `.catch(() => {})` на Share-кнопке + захардкоженный `/en/` в share-ссылке** — украинский пользователь делился английской версией. Переписал на `async` + try/catch с `console.warn` + toast на fail, URL теперь строится по текущему `language`. (3) **`handleDownloadEpub`/`handleShare` в `my-books/[id].tsx`** — оба silent `} catch {}`. EPUB теперь делает `Linking.canOpenURL` pre-check (на устройствах без EPUB-reader'а можем сразу показать toast "Can't open EPUB download"), при реальной ошибке `console.warn` + toast. Share залогируется в `console.warn` но без toast — user-dismiss Share-sheet на iOS это штатный reject, его не надо показывать как error. A11y на все три `secondaryBtn` в my-books: `role="button"` + осмысленный label + `state.selected` на Mark-as-read. Убрал unused `Linking`/`getApiConfig` импорты из `book/[slug].tsx`. |
| B-79 | WordCard web-UX parity | `src/components/WordCard.tsx`, `app/reader/[bookSlug]/[chapterSlug].tsx` | Мобильный `WordCard` исторически был проще web-аналога (`apps/web/src/components/reader/WordPopup.tsx`): не показывал фонетику, не показывал inline-определение (требовал отдельный тап в `DictionarySheet`), не давал удалить уже сохранённое слово без ухода в экран словаря, и не показывал, на какой язык идёт перевод. Пользователь прислал референс-скриншот web-варианта и попросил паритет. Полностью переписал WordCard: (1) header-строка `word + phonetic + stage-badge + close ×` — фонетика берётся из `dictionaryApi.lookupWord(fromLang, word)` с cancellation flag (404/нет-в-словаре — тихо игнорится); (2) **translation row** (orange accent `#C4704B`, matching web) с `ActivityIndicator` во время fetch; (3) **definition row** — первая definition из dictionary с префиксом `(partOfSpeech)`; (4) **actions row**: TTS-toggle → "Saved to vocabulary" status (если `wordSaved`) → Ignore-кнопка (trash-outline, рендерится при `isAuthenticated && wordSaved && onRemove` — аналог web delete-btn) → Save/Known CTA; (5) **footer**: `"Translating to {nativeName} · Change"` с `LanguagePickerModal` — при открытии picker-а auto-dismiss таймер отменяется (иначе карточка исчезала посреди выбора языка). В reader добавлен `handleRemoveWord`: optimistic snapshot → удаляем из `vocabMapRef.current` → `markVocabWords(updatedMap)` в WebView → `vocabularyApi.deleteWord(id)`; на fail rollback'им map + setWordSaved(true) + toast "Could not remove word. Try again." (нет отдельного `removeVocabWord` JS-хелпера, но `markVocabWords` перерисовывает подчёркивания с нуля — достаточно). Call-site WordCard в reader: убрал пропсы `onDictionary`/`onHighlight` (их роль теперь несут inline-definition + SelectionActionBar для multi-word), добавил `onRemove={handleRemoveWord}`. Все хуки (`useState`/`useEffect`/`useRef`/`useCallback`) помещены ДО любых условных рендеров — защита от регрессии B-77. A11y: close-btn, TTS toggle, Ignore, Save, MarkKnown, Change — все с `accessibilityRole="button"` + осмысленный label, TTS — c `accessibilityState.selected`. `tsc --noEmit` → exit 0. |

---

## 2. Открытые дефекты

*Нет открытых дефектов P0–P3.* Все пункты из предыдущих срезов закрыты, см. таблицу выше (B-22..B-79). `tsc --noEmit` по всему `apps/mobile` проходит чисто.

**Note по B-77**: пользователь сообщил видимую регрессию (нативное меню вместо `WordCard`, `WordCard` не появлялся, перевод не работал, ErrorBoundary "Rendered more hooks than during the previous render"). Корень — placement двух `useMemo` после ранних `if (loading) return` / `if (!chapter) return` в reader (введено в B-36). Хуки подняты выше, фикс верифицирован `tsc --noEmit`.

**Note по B-79**: после стабилизации B-77 пользователь явно запросил web-parity для `WordCard` — фонетика в header'е, inline definition, language footer с picker'ом ("делай WordCard до web-UX"). B-79 это паритет-апгрейд (не bug-fix): мобильная карточка теперь содержит ту же информацию, что и web-вариант, и поддерживает Ignore (удаление сохранённого слова) прямо из reader-а без захода в DictionarySheet/Vocabulary-экран. Ничего существующего не потеряно — verified vs commit `1d9d3a7` исторический мобильный WordCard был проще web-аналога; все убранные пропсы (`onDictionary`/`onHighlight`) дублировались SelectionActionBar для multi-word selection.

---

## 3. Риски и области для более глубокой проверки

- ~~**`lib/api.ts` / refresh-token**~~ — закрыто B-34 (single-flight promise, latch против флуда, network-fail ≠ terminal).
- ~~**`useReadingSession`**~~ — закрыто B-35 (`clearAutoEndTimer`, `resetSessionState`, per-book `sessionKey`).
- ~~**`ThemeContext`**~~ — закрыто B-36 (`useMemo` на buildReaderHtml + webViewSource).
- ~~**Offline-first**~~ — закрыто B-38/B-39/B-40 (`listCachedChapters`, reader cancellation + error UI, book detail offline fallback с баннером).
- ~~**Auth logout cleanup**~~ — закрыто B-37 (in-flight downloads отменяются, `downloads`/`cachedBooks` state сбрасываются).
- ~~**EAS/Android EPUB-экспорт**~~ — закрыто B-76. При аудите оказалось, что мобилка не использует `expo-sharing`/`expo-file-system` вообще — `Linking.openURL` в системный браузер. Реальные проблемы оказались другими: public-book deprecated кнопка (align с web), silent catches, `/en/` захардкожено в Share URL.

---

## 4. Статус

Все ранее открытые P0–P3 исправлены в порядке: **P0-1 → P1-1 → P1-2 → P1-3 → P1-4 → P1-5 → P1-6 → P2-1 → P2-3 → P2-4 → P3-1 → P3-4**. Второй проход — senior-ревью рисков из §3: **R-1 (B-33/B-34) → R-2 (B-35) → R-3 (B-36) → R-5 (B-37) → R-4 (B-38/B-39/B-40)**. Третий проход — senior-аудит hooks/контекстов/UX-мелочей: **B-41 (login polish) → B-42 (vocab reminder race) → B-43 (logout cache leak) → B-44 (TTS lang) → B-45 (TTS unmount) → B-46 (vocab review unmount guard) → B-47 (QuickStats cross-user) → B-48 (reader settings race) → B-49 (a11y sweep по шести sheets/модалкам)**. Четвёртый проход — senior-аудит blog/highlights/profile/vocab/library на cancellation + optimistic UI + silent-catch sweep: **B-50 (blog detail race + like rollback) → B-51 (blog list onEndReached + generation) → B-52 (highlights review unmount) → B-53 (profile name autofill + a11y) → B-54 (vocab optimistic delete + loadingMore guard) → B-55 (library runAction helper + optimistic remove + polling/quota logging)**. Пятый проход — senior-аудит user-book reader / stats / home / highlights / list-screens на memoization + cancellation + debounce: **B-56 (user-book reader HTML/source memoization) → B-57 (user-book reader silent catches) → B-58 (stats Promise.all cancellation + goal toasts) → B-59 (home loadContent/loadMoods cancellation) → B-60 (highlights index generation + loadingMore guard) → B-61 (books/authors 300ms debounce + cancellation)**. Шестой проход — senior-аудит detail-страниц / auth / reader silent-catches / render-side-effects: **B-62 (author/genre detail error UX + cancellation + language-aware share URL) → B-63 (genres list cancellation + loading reset) → B-64 (reset-password autofill + mountedRef) → B-65 (vocab review render-side haptic moved to effect) → B-66 (public reader silent-catch sweep: handleSaveWord / handleMarkKnown / handleHighlight / highlight-note save/delete получили toast + console.warn)**. Седьмой проход — senior-аудит refocus/refetch + reviews-поддеревье + animated-loop + dev-noise + широкий a11y-sweep: **B-67 (ContinueReadingCard useFocusEffect + a11y) → B-68 (reviews generation + loadingMore guard + rollback toast + a11y) → B-69 (ReaderTapCoachmark Animated.loop stop) → B-70 (_layout dev-gated logs + splash hide catch) → B-71 (PressableScale accessibilityState pass-through + home/vocab/focus/reminder a11y + cancellation polish)**. Восьмой проход — senior-аудит root-components (WordCard / StarRating / MoodSelector / LanguagePickerModal / ReaderSearchBar) на translation-race, optimistic-UI rollback и широкий a11y: **B-72 (WordCard translation cancellation + full a11y на expand/TTS/save/highlight/dictionary) → B-73 (StarRating optimistic snapshot/rollback + toast + per-star a11y + mountedRef) → B-74 (MoodSelector cancellation + explicit snapshot + toast + chip a11y) → B-75 (LanguagePickerModal header-role + row selected-state + ReaderSearchBar input/nav/close a11y)**. Девятый проход (закрытие R-6 — оказался ложной тревогой, реальные проблемы были другие): **B-76 (public-book EPUB кнопка убрана align с web-deprecation от 2026-04-15, Share URL теперь язык-aware, silent `} catch {}` на Linking/Share заменены на `console.warn` + toast + `Linking.canOpenURL` pre-check, a11y на secondary action buttons в `my-books/[id]`)**. Все риски R-1..R-6 закрыты. Десятый проход (критическая регрессия + feature-parity апгрейд по явному запросу пользователя): **B-77 (reader hooks-after-early-return — корень всех симптомов "нативное меню вместо WordCard / перевод не работает / ErrorBoundary"), B-78 (practice-карточка: mini Next button теперь `alignSelf: 'stretch'`), B-79 (WordCard переписан под web-UX: phonetic в header, inline definition, Ignore-кнопка для уже сохранённых, language footer с picker'ом, `handleRemoveWord` с optimistic+rollback+toast в reader)**. `tsc --noEmit` по всему `apps/mobile` проходит чисто после каждого батча.
