// Re-export of the auto-generated bundle. The real source is
// packages/reader-overlay/src/mobileBootstrap.ts; the bundling is done by
// apps/web/scripts/build-mobile-overlay.mjs and committed to
// readerOverlayScript.generated.ts so Metro/Expo can import it without a
// build step.
//
// To change overlay behavior, edit the shared package and run:
//   pnpm -C apps/web build:mobile-overlay
export { READER_OVERLAY_SCRIPT } from './readerOverlayScript.generated'
