# <One sentence naming what actually happened, not the component>

**Date:** YYYY-MM-DD
**Status:** Resolved | Mitigated | Open

## Impact

Who was affected, for how long, and how much. Numbers, not adjectives — "~389 books returned 404 for
five weeks", not "SEO was degraded". If the blast radius was measured, say how it was measured.

## Root cause

The mechanism, not the component. Name the specific thing that made a correct-looking decision wrong.
If a previous change triggered it, name that change.

## Fix

What shipped, with the PR number. Then the harder question: does this close the *instance* or the
*class*? Say which, explicitly.

## Lesson

The generalizable part. Written so it is useful to someone who has never seen this subsystem.

## Detection

How it was found — and how long it took. If it was found by a screenshot, a user, or by accident,
say so; that is the most important sentence in the file. What alarm would have caught it on day one,
and does that alarm exist now?

---

## Full write-up

Optional. The long-form record: what was ruled out, what the wrong hypothesis was, what the
misleading evidence looked like. Preserve dead ends — they are the reason the next person will not
repeat them.
