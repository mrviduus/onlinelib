# Android, прогон 4 — проверка #499 / #500 / #501

**Дата:** 2026-08-30 · **Сборка:** Play Internal Testing, versionCode 22 + OTA
`01a0513f-f38f-759c-9a7c-8b25a7ed79f5` (проверено в логе `dev.expo.updates`: скачан на первом
запуске, применён на втором) · **Бэкенд:** прод · **Устройство:** Pixel 7 Pro, API 37 ·
**Аккаунт:** `qa-android-p02@textstack.app`

---

## Вердикт

**Можно отдавать тестерам.** P0-3 закрыт, проверен на том сценарии, который его ловил, и в
трёх более жёстких вариантах. Блокеров не осталось.

---

## P0-3 — уход из книги в первую секунду · **Pass**

Прогресс набран бесконечной прокруткой до 42.6 % (Act III), строка на сервере
`scroll:4-act-iii:3595`.

| действие | locator | percent |
|---|---|---|
| базовая точка | `4-act-iii:3595` | 0.4264 |
| вход и выход за 1 с, №1 | `4-act-iii:3595` | 0.4264 |
| №2 | `4-act-iii:3595` | 0.4264 |
| №3 | `4-act-iii:3335` | 0.4246 |
| №4 | `4-act-iii:3335` | 0.4246 |

Итог четырёх циклов: **−0.18 п.п.** Для сравнения, вчера один такой цикл давал −42 п.п.

Жёстче:

- **выход за 0.4 с, три раза** — `4-act-iii:3335` не сдвинулся ни разу;
- **сворачивание за 0.5–0.6 с, десять раз** — 55.56 % → 55.09 %, дрейф того же порядка, что
  и при обычном чтении;
- **холодный старт** (`force-stop` + запуск) — 55.09 % сохранились полностью.

Оба входа восстанавливают правильно: «Continue» с карточки библиотеки и «Continue Reading»
с экрана книги. Нижняя панель ридера, карточка в библиотеке и строка на сервере показывают
одно число — 55 % (`shots/r4-06-bars.png`).

Побочно закрылось наблюдение из прошлого отчёта: **экран книги теперь показывает процент**
(`shots/r4-05-detail-continue.png`), раньше там было только «Continue Reading».

Автоозвучка карточек после нового OTA работает (`AudioTrack piid:175`, 24 кГц, без касания).

---

## Одна аномалия, воспроизвести не удалось

Один раз из шестнадцати попыток сворачивание через 0.5 с сбросило смещение внутри главы:
`4-act-iii:3335` → `4-act-iii:0`, 42.46 % → 40.3 %. Глава осталась правильной, книга не
потерялась — откат на начало текущей главы, примерно две страницы.

Десять последующих попыток тем же способом на глубине 55 % повторить это не смогли. Записываю
как есть: видел один раз, воспроизвести не смог. Наблюдать; блокером не считаю.

---

## Мелочи

1. **Запись позиции на сервер отстаёт секунд на двадцать.** Прокрутил — экран показывает 40 %,
   строка на сервере ещё старая, через ~20 с догоняет. На поведение не влияет, но при отладке
   об этом стоит помнить, иначе легко принять задержку за потерю.
2. Прогресс не двигается назад: пролистал книгу до конца, вернулся прокруткой в начало — на
   сервере остались 99.93 %, а нижняя панель показывала 1 %. Похоже на «максимум прочитанного»
   по замыслу, но экран и строка при этом расходятся.
3. Дрейф ~130–200 px за возврат (~0.05–0.1 п.п.) — меньше страницы за десяток заходов.
